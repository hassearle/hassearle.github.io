/**
 * ------------------------------------------------------------------------
 * Global
 * ------------------------------------------------------------------------
 */

// Increase the number of items in the bag
function increaseBagItems() {
  $(".js-bag-items-number").text(parseInt($(".js-bag-items-number").text()) + 1);
}
